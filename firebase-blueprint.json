{
  "entities": {
    "User": {
      "title": "User",
      "description": "A registered user of Remix: AI Talk Radio",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "User's email address"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the user account was initialized"
        }
      },
      "required": ["email", "createdAt"]
    },
    "Show": {
      "title": "Show",
      "description": "A talk radio show generated by the user",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this show"
        },
        "title": {
          "type": "string",
          "description": "The title of the radio show"
        },
        "duration": {
          "type": "number",
          "description": "Duration of the show in seconds"
        },
        "summary": {
          "type": "string",
          "description": "Two-sentence summary of the show content"
        },
        "date": {
          "type": "string",
          "description": "Formatted date of generation"
        },
        "host": {
          "type": "string",
          "description": "Name of the show host"
        },
        "coverImage": {
          "type": "string",
          "description": "URL of the cover image"
        },
        "audioUrl": {
          "type": "string",
          "description": "URL of the mixed audio track"
        },
        "notesUrl": {
          "type": "string",
          "description": "URL/path of the generated show notes JSON"
        },
        "transcript": {
          "type": "array",
          "description": "List of transcript lines with start/end timestamps"
        },
        "shareId": {
          "type": "string",
          "description": "Unique ID for public sharing"
        },
        "shareUrl": {
          "type": "string",
          "description": "Dynamic web sharing URL"
        },
        "isUserGenerated": {
          "type": "boolean",
          "description": "Whether this show was generated on demand"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The server-backed timestamp of when the show was saved"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The server-backed timestamp of when the show was last updated"
        }
      },
      "required": [
        "userId",
        "title",
        "duration",
        "summary",
        "date",
        "host",
        "coverImage",
        "audioUrl",
        "transcript",
        "createdAt",
        "updatedAt"
      ]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile collection"
    },
    "/shows/{showId}": {
      "schema": "Show",
      "description": "Collection of all generated radio shows"
    }
  }
}
